# Upgrade to Java 21 (Manual Plan)

## Summary

This document describes a concise, manual plan to confirm and complete the upgrade of this project to Java 21. The repository already configures a Gradle toolchain for Java 21 and a successful build was observed with the Gradle wrapper.

## Current status

- **Configured:** `build.gradle` contains:

```
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}
```

- **Build & tests:** `.\\gradlew.bat clean build` ran successfully (BUILD SUCCESSFUL).

## Upgrade steps (manual)

1. **Install JDK 21 (Local)**: Install a JDK 21 distribution on your development machine so tools outside the Gradle toolchain (or other team members) can run with the same JDK.

   - Check current java:

     `java -version`

   - Windows (PowerShell) examples:

     - Using Chocolatey (if installed):

       ```powershell
       choco install temurin --version=21.0.0 -y
       refreshenv;
       java -version
       ```

     - Or download and install Temurin / Adoptium JDK 21 from the vendor site and set `JAVA_HOME` in System Environment Variables.

   - After install, ensure `JAVA_HOME` points to the JDK 21 installation and that `java -version` returns `21`.

2. **Gradle toolchain & wrapper (already set)**: The project uses the Gradle Java toolchain configured in `build.gradle`. The Gradle wrapper will attempt to use an installed JDK matching the toolchain or run with an appropriate runtime.

   - Verify gradle wrapper and Java used by it:

     ```powershell
     .\gradlew.bat -version
     .\gradlew.bat clean build
     ```

3. **Run full build and tests** (CI/local): Run `.\\gradlew.bat clean build` and examine test results and warnings. Address any deprecation or migration warnings.

4. **Automated code transforms (optional)**: If you want automated refactorings (e.g., replace deprecated APIs), consider using OpenRewrite or similar tools. This repo doesn't appear to need code changes for Java 21, but the process would be:

   - Run OpenRewrite recipes targeted at JDK upgrades.
   - Review changes and run tests.

5. **CI updates**: Update CI workflows to include JDK 21 in the matrix (GitHub Actions example): add `java-version: [17, 21]` or replace 17 with 21 depending on support.

6. **Documentation & compatibility notes**: Add a short note in `README.md` or `UPGRADE_TO_JAVA21.md` (this file) documenting that the project targets Java 21 and how to set up the environment.

7. **Commit & PR**: If you change files (CI config, docs), create a branch (e.g., `upgrade/java21`), commit with message `chore: target Java 21 (Gradle toolchain)` and open a PR. Include test results and instructions for reviewers.

8. **Rollback plan**: If issues are found, revert the branch or update `build.gradle` back to previous languageVersion (e.g., 17) and re-run tests.

## Notes and recommendations

- The project already uses the Gradle toolchain set to Java 21; no source-level changes were necessary for the codebase based on current build results.
- If you maintain downstream systems or CI images, ensure those environments have JDK 21 available.
- If you want, I can prepare a small PR that updates `README.md` and adds this `UPGRADE_TO_JAVA21.md`, and optionally update CI files. Say the word and I will create the branch and commit changes.

---

Generated by automation as a manual upgrade plan on request.
